{
  "name": "hr-self-service",
  "private": true,
  "version": "0.1.0",
  "description": "Kurumsal HR Self Servis uygulamasi ve konfigure edilebilir HR workflow yonetimi",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:web",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "db:reset": "npm run db:reset --workspace server",
    "db:seed": "npm run db:seed --workspace server",
    "test": "npm run test --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web",
    "gen:secret": "node -e \"console.log(require('crypto').randomBytes(48).toString('base64url'))\"",
    "start": "npm run start --workspace server",
    "deploy:prepare": "npm run build && npm run db:deploy --workspace server && npm run db:seed --workspace server"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=20"
  }
}
